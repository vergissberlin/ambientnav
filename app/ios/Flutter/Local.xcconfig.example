// Copy to Local.xcconfig and set your Apple Developer Team ID.
// Find it in Xcode → Settings → Accounts → [Apple ID] → Team ID
// Or after selecting a team: Runner → Signing & Capabilities → Team.
DEVELOPMENT_TEAM = YOUR_TEAM_ID
