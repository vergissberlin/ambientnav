A small uppercase mono label — use for status, connection state, modes, and hardware tags.

```jsx
<Badge tone="cyan" glow>Live</Badge>
<Badge tone="neutral">2× ESP32</Badge>
```

Tones: `neutral`, `cyan`, `violet`, `magenta`. Set `glow` for active/live states. Keep text short (1–3 words); it's always uppercase mono.
